### 환경설정
윈도우 10
아나콘다 환경 생성 python=3.6

```
# pytorch 설치
pip install torch==1.6.0+cu101 torchvision==0.7.0+cu101 -f https://download.pytorch.org/whl/torch_s
table.html

pip install matplotlib

pip install gym
conda install -c conda-forge pystan
pip install Box2D
conda install -c conda-forge pyglet
pip install atari-py
```